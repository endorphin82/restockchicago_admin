query ProductsByCategoryId($categoryId: ID!){
    productsByCategoryId(categoryId: $categoryId){
        id
        name
        price
        images
        icon
        category{
            id
            name
            icons
        }
    }
}